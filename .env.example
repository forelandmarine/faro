# Contact form (optional - form logs to console without these)
RESEND_API_KEY=
CONTACT_EMAIL=hello@faro.is

# Plausible analytics (optional - script does not load unless set)
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=faro.is
